# infra/environments/shared/backend.config.example
# Copy to backend.config and fill in actual values

bucket       = "your-terraform-state-bucket"
key          = "shared/terraform.tfstate"
region       = "us-east-1"
use_lockfile = true
