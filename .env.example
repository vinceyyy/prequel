# AWS Configuration (for build scripts and local development)
AWS_REGION=your-aws-region
AWS_PROFILE=your-aws-profile

# Project Configuration (used for resource naming in build scripts)
PROJECT_PREFIX=prequel
ENVIRONMENT=dev  # Options: dev, staging, prod (must match infra/terraform.tfvars)

# Local Development Only (production values set in Terraform ECS task definition)
ENABLE_AUTH=true  # Set to false to disable authentication in local dev
AUTH_PASSCODE=your-secure-passcode  # Shared passcode for local development
LOG_LEVEL=info  # Options: debug, info, warn, error (default: info)
