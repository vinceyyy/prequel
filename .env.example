# AWS Configuration (REQUIRED for local development)
AWS_REGION=us-east-1
AWS_PROFILE=your-aws-sso-profile  # Must match your `aws configure sso` profile

# Project Configuration (CRITICAL: must match deployed infrastructure)
PROJECT_PREFIX=your-project-prefix  # Must match infra/terraform.tfvars project_prefix
ENVIRONMENT=dev                     # Must match infra/terraform.tfvars environment

# Domain Configuration
DOMAIN_NAME=your-domain.com  # Optional: for interview URLs (interview-id.domain.com)

# Local Development Only (production values set in Terraform ECS task definition)
ENABLE_AUTH=true                    # Set to false to disable authentication in local dev
AUTH_PASSCODE=your-secure-passcode  # Shared passcode for local development
LOG_LEVEL=info                      # Options: debug, info, warn, error (default: info)

# Advanced Configuration (auto-generated - usually not needed)
# INTERVIEWS_TABLE_NAME={PROJECT_PREFIX}-{ENVIRONMENT}-interviews
# OPERATIONS_TABLE_NAME={PROJECT_PREFIX}-{ENVIRONMENT}-operations
# TERRAFORM_STATE_BUCKET={PROJECT_PREFIX}-{ENVIRONMENT}-terraform-state
