# Portal Environment Configuration Template
# Copy this file to .env.local and update with your actual values

# ============================================================================
# AWS Configuration
# ============================================================================
# AWS profile for SSO authentication (local development only)
# In ECS, credentials are provided by IAM task roles automatically
AWS_PROFILE=your-aws-profile
AWS_REGION=us-east-1

# ============================================================================
# Project Configuration
# ============================================================================
# CRITICAL: These must match your deployed infrastructure in infra/terraform.tfvars
PROJECT_PREFIX=your-project-prefix  # Must match project_prefix in terraform.tfvars
ENVIRONMENT=dev                      # Must match environment in terraform.tfvars
DOMAIN_NAME=your-domain.com         # Must match domain_name in terraform.tfvars (optional)

# ============================================================================
# OpenAI Configuration (Local Development Only)
# ============================================================================
# Required for local development to provision interview instances
# In ECS deployment, this is provided via terraform remote state outputs
# Get these values from infra/terraform.tfvars
OPENAI_PROJECT_ID=proj_YourProjectId  # Must match openai_project_id in terraform.tfvars

# ============================================================================
# Logging Configuration
# ============================================================================
# Set to 'debug' for verbose scheduler logging
LOG_LEVEL=info  # Options: debug, info, warn, error

# ============================================================================
# Authentication (Optional - for local development)
# ============================================================================
# If you want to test authentication locally
# AUTH_PASSCODE=your-passcode  # Must match auth_passcode in terraform.tfvars

# ============================================================================
# Notes
# ============================================================================
# - DynamoDB table names are auto-generated: {PROJECT_PREFIX}-{ENVIRONMENT}-{table}
# - S3 bucket names are auto-generated: {PROJECT_PREFIX}-{ENVIRONMENT}-{bucket}
# - ECS cluster name is auto-generated: {PROJECT_PREFIX}-{ENVIRONMENT}
# - All values must match your deployed infrastructure for local development to work
